{
  "name": "react-webchat",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "dev": "lerna run --scope chat-client start & lerna run --scope chat-server serve",
    "start": "lerna run --scope chat-client build & lerna run --scope chat-server serve"
  }
}
